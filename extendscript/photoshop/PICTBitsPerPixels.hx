package jsx.photoshop;

/*
 * The number of bits per pixel to use when compressing a PICT file.
 */
typedef PICTBitsPerPixels = {
  /*
   * 2 bits per pixel.
   */
  var TWO:Int;
  /*
   * 4 bits per pixel.
   */
  var FOUR:Int;
  /*
   * 8 bits per pixel.
   */
  var EIGHT:Int;
  /*
   * 16 bits per pixel.
   */
  var SIXTEEN:Int;
  /*
   * 32 bits per pixel.
   */
  var THIRTYTWO:Int;
};
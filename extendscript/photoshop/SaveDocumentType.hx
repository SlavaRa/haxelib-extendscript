package extendscript.photoshop;

/*
 * Document formats that Photoshop can save to.
 */
typedef SaveDocumentType = {
  /*
   * Photoshop format.
   */
  var PHOTOSHOP:Int;
  /*
   * BMP format.
   */
  var BMP:Int;
  /*
   * CompuServe GIF format.
   */
  var COMPUSERVEGIF:Int;
  /*
   * EPS document produced by Photoshop.
   */
  var PHOTOSHOPEPS:Int;
  /*
   * JPEG format.
   */
  var JPEG:Int;
  /*
   * PCX format.
   */
  var PCX:Int;
  /*
   * PDF document produced by Photoshop.
   */
  var PHOTOSHOPPDF:Int;
  /*
   * PICT file format.
   */
  var PICTFileFORMAT:Int;
  /*
   * PICT resource format.
   */
  var PICTRESOURCEFORMAT:Int;
  /*
   * Pixar format.
   */
  var PIXAR:Int;
  /*
   * PNG format.
   */
  var PNG:Int;
  /*
   * Raw format.
   */
  var RAW:Int;
  /*
   * Scitex CT format.
   */
  var SCITEXCT:Int;
  /*
   * Targa format.
   */
  var TARGA:Int;
  /*
   * TIFF format.
   */
  var TIFF:Int;
  /*
   * Photoshop DCS 1.0 format.
   */
  var PHOTOSHOPDCS_1:Int;
  /*
   * Photoshop DCS 2.0 format.
   */
  var PHOTOSHOPDCS_2:Int;
  /*
   * Alias PIX format.
   */
  var ALIASPIX:Int;
  /*
   * Electric format.
   */
  var ELECTRICIMAGE:Int;
  /*
   * Portable Bitmap format.
   */
  var PORTABLEBITMAP:Int;
  /*
   * Wavefront RLA format.
   */
  var WAVEFRONTRLA:Int;
  /*
   * SGI RGB format.
   */
  var SGIRGB:Int;
  /*
   * SoftImage format.
   */
  var SOFTIMAGE:Int;
  /*
   * Wireless Bitmap format (WBMP)
   */
  var WIRELESSBITMAP:Int;
};
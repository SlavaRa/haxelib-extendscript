package jsx.photoshop;

/*
 * The measurement upon which to take action.
 */
typedef MeasurementRange = {
  /*
   * All measurements.
   */
  var ALLMEASUREMENTS:Int;
  /*
   * Only active measurements.
   */
  var ACTIVEMEASUREMENTS:Int;
};
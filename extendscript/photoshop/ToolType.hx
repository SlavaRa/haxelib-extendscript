package extendscript.photoshop;

/*
 * Tools for the stroke path command.
 */
typedef ToolType = {
  /*
   * Pencil.
   */
  var PENCIL:Int;
  /*
   * Brush.
   */
  var BRUSH:Int;
  /*
   * Eraser.
   */
  var ERASER:Int;
  /*
   * Background eraser.
   */
  var BACKGROUNDERASER:Int;
  /*
   * Clone stamp.
   */
  var CLONESTAMP:Int;
  /*
   * Pattern stamp.
   */
  var PATTERNSTAMP:Int;
  /*
   * Healing brush.
   */
  var HEALINGBRUSH:Int;
  /*
   * History brush.
   */
  var HISTORYBRUSH:Int;
  /*
   * Art history brush.
   */
  var ARTHISTORYBRUSH:Int;
  /*
   * Smudge.
   */
  var SMUDGE:Int;
  /*
   * Blur.
   */
  var BLUR:Int;
  /*
   * Sharpen.
   */
  var SHARPEN:Int;
  /*
   * Dodge.
   */
  var DODGE:Int;
  /*
   * Burn.
   */
  var BURN:Int;
  /*
   * Sponge.
   */
  var SPONGE:Int;
  /*
   * Color replacement tool.
   */
  var COLORREPLACEMENTTOOL:Int;
};
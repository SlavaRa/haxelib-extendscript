package extendscript.photoshop;

/*
 * The resolution to use when saving an image in Targa format.
 */
typedef TargaBitsPerPixels = {
  /*
   * 16 bits per pixel.
   */
  var SIXTEEN:Int;
  /*
   * 24 bits per pixel.
   */
  var TWENTYFOUR:Int;
  /*
   * 32 bits per pixel.
   */
  var THIRTYTWO:Int;
};
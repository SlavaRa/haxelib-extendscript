package extendscript.photoshop;

/*
 * Deprecated for Adobe Photoshop CS3, the make photo gallery method has moved to Bridge.
 */
typedef GalleryCustomColorOptions = {
  /*
   * Background color.
   */
  var backgroundColor:RGBColor;
  /*
   * Banner color.
   */
  var bannerColor:RGBColor;
  /*
   * Text color.
   */
  var textColor:RGBColor;
  /*
   * Active link color.
   */
  var activeLinkColor:RGBColor;
  /*
   * Link color.
   */
  var linkColor:RGBColor;
  /*
   * Visited link color.
   */
  var visitedLinkColor:RGBColor;
};
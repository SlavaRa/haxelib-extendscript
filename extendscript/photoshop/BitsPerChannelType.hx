package jsx.photoshop;

/*
 * The number of bits per color channel.
 */
typedef BitsPerChannelType = {
  /*
   * 1 bit per channel.
   */
  var ONE:Int;
  /*
   * 8 bits per channel.
   */
  var EIGHT:Int;
  /*
   * 16 bits per channel.
   */
  var SIXTEEN:Int;
  /*
   * 32 bits per channel.
   */
  var THIRTYTWO:Int;
};
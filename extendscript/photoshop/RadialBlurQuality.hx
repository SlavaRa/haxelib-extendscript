package jsx.photoshop;

/*
 * The radial blur quality.
 */
typedef RadialBlurQuality = {
  /*
   * Produces fast but grainy results.
   */
  var DRAFT:Int;
  /*
   * Produces good results.
   */
  var GOOD:Int;
  /*
   * Produces best results.
   */
  var BEST:Int;
};
package extendscript.photoshop;

/*
 * The type of text.
 */
typedef TextType = {
  /*
   * Text that does not wrap.
   */
  var POINTTEXT:Int;
  /*
   * Text that wraps within a bounding box.
   */
  var PARAGRAPHTEXT:Int;
};
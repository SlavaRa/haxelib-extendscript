package jsx.photoshop;

/*
 * Describes a point. This class is also a two-element collection.
 */
typedef Point = {
  /*
   * The left coordinate.
   */
  var x:Float;
  /*
   * The top coordinate.
   */
  var y:Float;
  /*
   * The left coordinate.
   */
  var left:Float;
  /*
   * The top coordinate.
   */
  var top:Float;
  /*
   * The array length.
   */
  var length:Float;
};
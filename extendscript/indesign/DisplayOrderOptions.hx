package extendscript.indesign;

/*
 * Values to specify the order table cells will display in story and galley views.
 */
typedef DisplayOrderOptions = {
  /*
   * Order by rows.
   */
  var ORDER_BY_ROWS:Float;
  /*
   * Order by columns.
   */
  var ORDER_BY_COLUMNS:Float;
};
package jsx.indesign;

/*
 * The order in which to arrange records in the target document.
 */
typedef ArrangeBy = {
  /*
   * Arranges records by row.
   */
  var ROWS_FIRST:Float;
  /*
   * Arranges records by column.
   */
  var COLUMNS_FIRST:Float;
};
package jsx.indesign;

/*
 * Options for export order list.
 */
typedef NumberedListExportOption = {
  /*
   * map to html ordered list.
   */
  var ORDERED_LIST:Float;
  /*
   * map to html static ordered list.
   */
  var STATIC_ORDERED_LIST:Float;
  /*
   * convert to text.
   */
  var AS_TEXT:Float;
};
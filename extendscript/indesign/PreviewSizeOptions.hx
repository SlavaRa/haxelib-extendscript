package extendscript.indesign;

/*
 * Preview size options.
 */
typedef PreviewSizeOptions = {
  /*
   * Small preview (128 x 128).
   */
  var SMALL:Float;
  /*
   * Medium preview (256 x 256).
   */
  var MEDIUM:Float;
  /*
   * Large preview (512 x 512).
   */
  var LARGE:Float;
  /*
   * Extra large preview (1024 x 1024).
   */
  var EXTRA_LARGE:Float;
};
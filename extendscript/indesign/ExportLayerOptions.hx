package extendscript.indesign;

/*
 * Export layer options.
 */
typedef ExportLayerOptions = {
  /*
   * Export all layers
   */
  var EXPORT_ALL_LAYERS:Float;
  /*
   * Export visible layers
   */
  var EXPORT_VISIBLE_LAYERS:Float;
  /*
   * Export visible and printable layers
   */
  var EXPORT_VISIBLE_PRINTABLE_LAYERS:Float;
};
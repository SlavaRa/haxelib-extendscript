package jsx.indesign;

/*
 * Tag form options.
 */
typedef TagTextForm = {
  /*
   * Displays tags in long form; creates larger text files.
   */
  var VERBOSE:Float;
  /*
   * Abbreviates tags; creates smaller text files.
   */
  var ABBREVIATED:Float;
};
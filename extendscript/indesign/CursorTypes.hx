package extendscript.indesign;

/*
 * Cursor types.
 */
typedef CursorTypes = {
  /*
   * Uses a standard cursor.
   */
  var STANDARD_CURSOR:Float;
  /*
   * Uses a thick cursor.
   */
  var THICK_CURSOR:Float;
  /*
   * Uses a barbell cursor.
   */
  var BARBELL_CURSOR:Float;
  /*
   * Uses a block cursor.
   */
  var BLOCK_CURSOR:Float;
};
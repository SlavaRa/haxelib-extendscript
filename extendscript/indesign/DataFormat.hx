package extendscript.indesign;

/*
 * Format options for image data.
 */
typedef DataFormat = {
  /*
   * Uses ASCII format.
   */
  var ASCII:Float;
  /*
   * Uses binary format.
   */
  var BINARY:Float;
};
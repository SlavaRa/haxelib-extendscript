package jsx.indesign;

/*
 * Ruby type options.
 */
typedef RubyTypes = {
  /*
   * Provides ruby for a group of characters.
   */
  var GROUP_RUBY:Float;
  /*
   * Provides ruby for each individual character in the group.
   */
  var PER_CHARACTER_RUBY:Float;
};
package extendscript.indesign;

/*
 * Color-management system source options.
 */
typedef SourceSpaces = {
  /*
   * Uses the color space of the document.
   */
  var USE_DOCUMENT:Float;
  /*
   * Uses the color space of the proof.
   */
  var PROOF_SPACE:Float;
};
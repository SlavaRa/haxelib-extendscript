package extendscript.indesign;

/*
 * Options for export unorder list.
 */
typedef BulletListExportOption = {
  /*
   * map to html unordered list.
   */
  var UNORDERED_LIST:Float;
  /*
   * convert to text.
   */
  var AS_TEXT:Float;
};
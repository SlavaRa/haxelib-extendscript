package extendscript.indesign;

/*
 * Numbered paragraphs options.
 */
typedef NumberedParagraphsOptions = {
  /*
   * Includes the full paragraph text.
   */
  var INCLUDE_FULL_PARAGRAPH:Float;
  /*
   * Includes only the paragraph number.
   */
  var INCLUDE_NUMBERS_ONLY:Float;
  /*
   * Excludes paragraph numbers.
   */
  var EXCLUDE_NUMBERS:Float;
};
package extendscript.indesign;

/*
 * Note background color options.
 */
typedef NoteBackgrounds = {
  /*
   * Uses the galley background color.
   */
  var GALLEY_BACKGROUND_COLOR:Float;
  /*
   * Uses the note color.
   */
  var USE_NOTE_COLOR:Float;
};
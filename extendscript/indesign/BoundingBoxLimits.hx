package extendscript.indesign;

/*
 * Bounding box limits.
 */
typedef BoundingBoxLimits = {
  /*
   * Outer stroke bounds
   */
  var OUTER_STROKE_BOUNDS:Float;
  /*
   * Geometric path bounds
   */
  var GEOMETRIC_PATH_BOUNDS:Float;
};
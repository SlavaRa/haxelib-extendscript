package jsx.indesign;

/*
 * Hyperlink border weight options.
 */
typedef HyperlinkAppearanceWidth = {
  /*
   * Uses a thin border.
   */
  var THIN:Float;
  /*
   * Uses a medium border.
   */
  var MEDIUM:Float;
  /*
   * Uses a thick border.
   */
  var THICK:Float;
};
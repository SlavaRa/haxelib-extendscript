package extendscript.indesign;

/*
 * End join types.
 */
typedef EndJoin = {
  /*
   * Miter end join.
   */
  var MITER_END_JOIN:Float;
  /*
   * Rounded end join.
   */
  var ROUND_END_JOIN:Float;
  /*
   * Beveled end join.
   */
  var BEVEL_END_JOIN:Float;
};
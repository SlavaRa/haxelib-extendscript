package jsx.indesign;

/*
 * Mapping type for style mappings.
 */
typedef MapType = {
  /*
   * style to style mapping rule.
   */
  var STYLE_MAPPING_RULE:Float;
  /*
   * group to group mapping rule.
   */
  var GROUP_MAPPING_RULE:Float;
  /*
   * style to group mapping rule.
   */
  var STYLE_TO_GROUP_MAPPING_RULE:Float;
  /*
   * group to style mapping rule.
   */
  var GROUP_TO_STYLE_MAPPING_RULE:Float;
};
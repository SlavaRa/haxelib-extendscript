package extendscript.indesign;

/*
 * Options for page color label.
 */
typedef PageColorOptions = {
  /*
   * No color.
   */
  var NOTHING:Float;
  /*
   * Uses the color label of the page's master page.
   */
  var USE_MASTER_COLOR:Float;
};
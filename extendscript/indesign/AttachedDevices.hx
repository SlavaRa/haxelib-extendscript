package extendscript.indesign;

/*
 * The device types that are attached.
 */
typedef AttachedDevices = {
  /*
   * all devices, Android followed by iOS.
   */
  var ALL:Float;
  /*
   * Android devices.
   */
  var ANDROID:Float;
  /*
   * iOS devices.
   */
  var IOS:Float;
};
package extendscript.indesign;

/*
 * Options for preview pages.
 */
typedef PreviewPagesOptions = {
  /*
   * First page.
   */
  var FIRST_PAGE:Float;
  /*
   * First two pages.
   */
  var FIRST_2_PAGES:Float;
  /*
   * First five pages.
   */
  var FIRST_5_PAGES:Float;
  /*
   * First ten pages.
   */
  var FIRST_10_PAGES:Float;
  /*
   * All pages.
   */
  var ALL_PAGES:Float;
};
package extendscript.indesign;

/*
 * The default value.
 */
typedef AutoEnum = {
  /*
   * Uses the default value defined automatically for the object based on a parent or other type of object.
   */
  var AUTO_VALUE:Float;
};
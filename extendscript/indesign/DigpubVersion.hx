package extendscript.indesign;

/*
 * The versions that are available.
 */
typedef DigpubVersion = {
  /*
   * plugin, folio and plist versions in that order.
   */
  var ALL:Float;
  /*
   * plugin version.
   */
  var PLUGIN:Float;
  /*
   * folio version.
   */
  var FOLIO:Float;
  /*
   * plist version.
   */
  var PLIST:Float;
};
package extendscript.indesign;

/*
 * Options for incorporating imported XML content.
 */
typedef XMLImportStyles = {
  /*
   * Appends the imported content.
   */
  var APPEND_IMPORT:Float;
  /*
   * Merges the imported content.
   */
  var MERGE_IMPORT:Float;
};
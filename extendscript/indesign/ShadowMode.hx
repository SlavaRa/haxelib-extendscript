package extendscript.indesign;

/*
 * Shadow mode options.
 */
typedef ShadowMode = {
  /*
   * Does not use a shadow.
   */
  var NONE:Float;
  /*
   * Uses a standard blurred drop shadow.
   */
  var DROP:Float;
};
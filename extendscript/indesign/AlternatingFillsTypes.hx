package extendscript.indesign;

/*
 * Pattern options for alternating fills.
 */
typedef AlternatingFillsTypes = {
  /*
   * No alternating pattern.
   */
  var NO_ALTERNATING_PATTERN:Float;
  /*
   * Alternates row fills.
   */
  var ALTERNATING_ROWS:Float;
  /*
   * Alternates column fills.
   */
  var ALTERNATING_COLUMNS:Float;
};
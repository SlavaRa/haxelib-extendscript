package extendscript.indesign;

/*
 * Flattener level override options.
 */
typedef SpreadFlattenerLevel = {
  /*
   * Uses the default level.
   */
  var DEFAULT_VALUE:Float;
  /*
   * Turns off flattening.
   */
  var NONE:Float;
  /*
   * Uses the specified custom flattening level.
   */
  var CUSTOM:Float;
};
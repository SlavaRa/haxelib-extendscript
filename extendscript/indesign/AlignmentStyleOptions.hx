package extendscript.indesign;

/*
 * Alignment options.
 */
typedef AlignmentStyleOptions = {
  /*
   * Preserves the spreadsheet's alignment.
   */
  var SPREADSHEET:Float;
  /*
   * Left aligns cells.
   */
  var LEFT_ALIGN:Float;
  /*
   * Right aligns cells.
   */
  var RIGHT_ALIGN:Float;
  /*
   * Center aligns cells.
   */
  var CENTER_ALIGN:Float;
};
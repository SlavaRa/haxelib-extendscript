package extendscript.indesign;

/*
 * The page transition type options.
 */
typedef PageTransitionTypeOptions = {
  /*
   * No page transition applied.
   */
  var NONE:Float;
  /*
   * The Blinds page transition.
   */
  var BLINDS_TRANSITION:Float;
  /*
   * The Blinds page transition.
   */
  var BOX_TRANSITION:Float;
  /*
   * The Comb page transition.
   */
  var COMB_TRANSITION:Float;
  /*
   * The Cover page transition.
   */
  var COVER_TRANSITION:Float;
  /*
   * The Dissolve page transition.
   */
  var DISSOLVE_TRANSITION:Float;
  /*
   * The Fade page transition.
   */
  var FADE_TRANSITION:Float;
  /*
   * The Page Turn page transition.
   */
  var PAGE_TURN_TRANSITION:Float;
  /*
   * The Push page transition.
   */
  var PUSH_TRANSITION:Float;
  /*
   * The Split page transition.
   */
  var SPLIT_TRANSITION:Float;
  /*
   * The Uncover page transition.
   */
  var UNCOVER_TRANSITION:Float;
  /*
   * The Wipe page transition.
   */
  var WIPE_TRANSITION:Float;
  /*
   * The Zoom In page transition.
   */
  var ZOOM_IN_TRANSITION:Float;
  /*
   * The Zoom Out page transition.
   */
  var ZOOM_OUT_TRANSITION:Float;
};
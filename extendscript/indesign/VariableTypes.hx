package extendscript.indesign;

/*
 * Text variable types.
 */
typedef VariableTypes = {
  /*
   * Custom text variable.
   */
  var CUSTOM_TEXT_TYPE:Float;
  /*
   * File name variable.
   */
  var FILE_NAME_TYPE:Float;
  /*
   * Last page number variable.
   */
  var LAST_PAGE_NUMBER_TYPE:Float;
  /*
   * Chapter number variable.
   */
  var CHAPTER_NUMBER_TYPE:Float;
  /*
   * Output date variable.
   */
  var OUTPUT_DATE_TYPE:Float;
  /*
   * Creation date variable.
   */
  var CREATION_DATE_TYPE:Float;
  /*
   * Modification date variable.
   */
  var MODIFICATION_DATE_TYPE:Float;
  /*
   * Running header (character style) variable.
   */
  var MATCH_CHARACTER_STYLE_TYPE:Float;
  /*
   * Running header (paragraph style) variable.
   */
  var MATCH_PARAGRAPH_STYLE_TYPE:Float;
  /*
   * Private cross reference page number variable.
   */
  var XREF_PAGE_NUMBER_TYPE:Float;
  /*
   * Private cross reference chapter number variable.
   */
  var XREF_CHAPTER_NUMBER_TYPE:Float;
  /*
   * Live Caption variable.
   */
  var LIVE_CAPTION_TYPE:Float;
};
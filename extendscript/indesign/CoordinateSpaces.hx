package jsx.indesign;

/*
 * Coordinate space options.
 */
typedef CoordinateSpaces = {
  /*
   * Pasteboard coordinates
   */
  var PASTEBOARD_COORDINATES:Float;
  /*
   * Parent coordinates
   */
  var PARENT_COORDINATES:Float;
  /*
   * Inner coordinates
   */
  var INNER_COORDINATES:Float;
  /*
   * Page coordinates.
   */
  var PAGE_COORDINATES:Float;
  /*
   * Spread coordinates.
   */
  var SPREAD_COORDINATES:Float;
};
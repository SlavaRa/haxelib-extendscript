package jsx.indesign;

/*
 * The enumeration of tool box tools.
 */
typedef UITools = {
  /*
   * No selection
   */
  var NONE:Float;
  /*
   * The selection tool
   */
  var SELECTION_TOOL:Float;
  /*
   * The direct selection tool
   */
  var DIRECT_SELECTION_TOOL:Float;
  /*
   * The gap tool
   */
  var GAP_TOOL:Float;
  /*
   * The pen tool
   */
  var PEN_TOOL:Float;
  /*
   * The add anchor point tool
   */
  var ADD_ANCHOR_POINT:Float;
  /*
   * The delete anchor point tool
   */
  var DELETE_ANCHOR_POINT:Float;
  /*
   * The convert direction point tool
   */
  var CONVERT_DIRECTION_POINT:Float;
  /*
   * The line tool
   */
  var LINE_TOOL:Float;
  /*
   * The type tool
   */
  var TYPE_TOOL:Float;
  /*
   * The type on a path tool
   */
  var TYPE_ON_PATH_TOOL:Float;
  /*
   * The pencil tool
   */
  var PENCIL_TOOL:Float;
  /*
   * The smooth tool
   */
  var SMOOTH_TOOL:Float;
  /*
   * The erase tool
   */
  var ERASE_TOOL:Float;
  /*
   * The polygon frame tool
   */
  var POLYGON_FRAME_TOOL:Float;
  /*
   * The rectangle frame tool
   */
  var RECTANGLE_FRAME_TOOL:Float;
  /*
   * The ellipse frame tool
   */
  var ELLIPSE_FRAME_TOOL:Float;
  /*
   * The polygon tool
   */
  var POLYGON_TOOL:Float;
  /*
   * The rectangle tool
   */
  var RECTANGLE_TOOL:Float;
  /*
   * The ellipse tool
   */
  var ELLIPSE_TOOL:Float;
  /*
   * The rotate tool
   */
  var ROTATE_TOOL:Float;
  /*
   * The scale tool
   */
  var SCALE_TOOL:Float;
  /*
   * The shear tool
   */
  var SHEAR_TOOL:Float;
  /*
   * The scissors tool
   */
  var SCISSORS_TOOL:Float;
  /*
   * The free transform tool
   */
  var FREE_TRANSFORM_TOOL:Float;
  /*
   * The gradient swatch tool
   */
  var GRADIENT_SWATCH_TOOL:Float;
  /*
   * The gradient feather tool
   */
  var GRADIENT_FEATHER_TOOL:Float;
  /*
   * The note tool
   */
  var NOTE_TOOL:Float;
  /*
   * The eye dropper tool
   */
  var EYE_DROPPER_TOOL:Float;
  /*
   * The measure tool
   */
  var MEASURE_TOOL:Float;
  /*
   * The hand tool
   */
  var HAND_TOOL:Float;
  /*
   * The zoom tool
   */
  var ZOOM_TOOL:Float;
  /*
   * The table creation tool
   */
  var TABLE_TOOL:Float;
  /*
   * The place cursor tool which gets set after an import via the Place command
   */
  var PLACE_CURSOR_TOOL:Float;
  /*
   * The motion path tool
   */
  var MOTION_PATH_TOOL:Float;
  /*
   * The page tool
   */
  var PAGE_TOOL:Float;
};
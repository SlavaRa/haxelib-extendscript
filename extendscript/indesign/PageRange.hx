package extendscript.indesign;

/*
 * Page range options.
 */
typedef PageRange = {
  /*
   * Print or export all pages in the document.
   */
  var ALL_PAGES:Float;
  /*
   * Export selected items in the document.
   */
  var SELECTED_ITEMS:Float;
};
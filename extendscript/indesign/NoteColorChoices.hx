package extendscript.indesign;

/*
 * Note color options.
 */
typedef NoteColorChoices = {
  /*
   * Uses the color assigned to the user.
   */
  var USE_USER_COLOR:Float;
  /*
   * Uses the note color.
   */
  var USE_NOTE_PREF_COLOR:Float;
};
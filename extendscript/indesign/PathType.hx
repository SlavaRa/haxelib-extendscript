package extendscript.indesign;

/*
 * Path type options.
 */
typedef PathType = {
  /*
   * The path is an open path.
   */
  var OPEN_PATH:Float;
  /*
   * The path is a closed path.
   */
  var CLOSED_PATH:Float;
};
package extendscript.indesign;

/*
 * Booklet type options.
 */
typedef BookletTypeOptions = {
  /*
   * Two up saddle stitch imposition.
   */
  var TWO_UP_SADDLE_STITCH:Float;
  /*
   * Two up perfect bound imposition.
   */
  var TWO_UP_PERFECT_BOUND:Float;
  /*
   * Two up consecutive imposition.
   */
  var TWO_UP_CONSECUTIVE:Float;
  /*
   * Three up consecutive imposition.
   */
  var THREE_UP_CONSECUTIVE:Float;
  /*
   * Four up consecutive imposition.
   */
  var FOUR_UP_CONSECUTIVE:Float;
};
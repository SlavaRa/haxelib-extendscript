package extendscript.indesign;

/*
 * Image Page Break Options.
 */
typedef ImagePageBreakType = {
  /*
   * Page break before image.
   */
  var PAGE_BREAK_BEFORE:Float;
  /*
   * Page break after image.
   */
  var PAGE_BREAK_AFTER:Float;
  /*
   * Page break before and after image.
   */
  var PAGE_BREAK_BEFORE_AND_AFTER:Float;
};
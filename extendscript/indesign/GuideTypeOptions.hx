package jsx.indesign;

/*
 * Guide type options for ruler guides.
 */
typedef GuideTypeOptions = {
  /*
   * Ruler guide (default).
   */
  var RULER:Float;
  /*
   * Liquid guide.
   */
  var LIQUID:Float;
};
package jsx.indesign;

/*
 * Sort order options
 */
typedef SortAssets = {
  /*
   * Sort by name
   */
  var BY_NAME:Float;
  /*
   * Sort oldest first
   */
  var BY_OLDEST:Float;
  /*
   * Sort newest first
   */
  var BY_NEWEST:Float;
  /*
   * Sort by type
   */
  var BY_TYPE:Float;
};
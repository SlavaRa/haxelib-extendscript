package jsx.indesign;

/*
 * The smooth scrolling option.
 */
typedef SmoothScrollingOptions = {
  /*
   * no smooth scrolling.
   */
  var NO_SMOOTH_SCROLL:Float;
  /*
   * vertical smooth scrolling.
   */
  var VERTICAL:Float;
  /*
   * horizontal smooth scrolling.
   */
  var HORIZONTAL:Float;
  /*
   * vertical and horizontal smooth scrolling.
   */
  var VERTICAL_AND_HORIZONTAL:Float;
};
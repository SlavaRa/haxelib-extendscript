package extendscript.indesign;

/*
 * Preflight layer options.
 */
typedef PreflightLayerOptions = {
  /*
   * Preflight all layers
   */
  var PREFLIGHT_ALL_LAYERS:Float;
  /*
   * Preflight visible layers
   */
  var PREFLIGHT_VISIBLE_LAYERS:Float;
  /*
   * Preflight visible and printable layers
   */
  var PREFLIGHT_VISIBLE_PRINTABLE_LAYERS:Float;
};
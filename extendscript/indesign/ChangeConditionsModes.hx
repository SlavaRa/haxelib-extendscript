package extendscript.indesign;

/*
 * Change conditions modes.
 */
typedef ChangeConditionsModes = {
  /*
   * Change replaces applied conditions.
   */
  var REPLACE_WITH:Float;
  /*
   * Change adds to applied conditions.
   */
  var ADD_TO:Float;
};
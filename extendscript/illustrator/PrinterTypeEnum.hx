package extendscript.illustrator;

/*
 * The printer type.
 */
typedef PrinterTypeEnum = {
  /*
   * Unknown printer type.
   */
  var Unknown:Int;
  /*
   * PostScript printer.
   */
  var POSTSCRIPTPRINTER:Int;
  /*
   * Non PostScript printer.
   */
  var NONPOSTSCRIPTPRINTER:Int;
};
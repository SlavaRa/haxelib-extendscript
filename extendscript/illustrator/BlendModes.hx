package extendscript.illustrator;

/*
 * Blend modes used when compositing an object.
 */
typedef BlendModes = {
  var NORMAL:Int;
  var MULTIPLY:Int;
  var SCREEN:Int;
  var OVERLAY:Int;
  var SOFTLIGHT:Int;
  var HARDLIGHT:Int;
  var COLORDODGE:Int;
  var COLORBURN:Int;
  var DARKEN:Int;
  var LIGHTEN:Int;
  var DIFFERENCE:Int;
  var EXCLUSION:Int;
  var HUE:Int;
  var SATURATIONBLEND:Int;
  var COLORBLEND:Int;
  var LUMINOSITY:Int;
};
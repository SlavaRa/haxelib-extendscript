package jsx.photoshop;

/*
 * The line style for nonprinting grids displayed over images.
 */
typedef GridLineStyle = {
  /*
   * Solid.
   */
  var SOLID:Int;
  /*
   * Dashed.
   */
  var DASHED:Int;
  /*
   * Dotted.
   */
  var DOTTED:Int;
};
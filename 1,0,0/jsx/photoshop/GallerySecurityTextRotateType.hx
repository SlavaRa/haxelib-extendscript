package jsx.photoshop;

/*
 * Deprecated for Adobe Photoshop CS3. web photo gallery security text rotation types.
 */
typedef GallerySecurityTextRotateType = {
  /*
   * No rotate.
   */
  var ZERO:Int;
  /*
   * Rotate 45 degrees clock wise.
   */
  var CLOCKWISE45:Int;
  /*
   * Rotate 90 degrees clock wise.
   */
  var CLOCKWISE90:Int;
  /*
   * Rotate 45 degrees counter clock wise.
   */
  var COUNTERCLOCKWISE45:Int;
  /*
   * Rotate 90 degrees counter clock wise.
   */
  var COUNTERCLOCKWISE90:Int;
};
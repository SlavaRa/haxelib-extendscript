package jsx.photoshop;

/*
 * Deprecated for Adobe Photoshop CS3.
 */
typedef PhotoCDColorSpace = {
  /*
   * RGB with 8 bits per channel.
   */
  var RGB8:Int;
  /*
   * RGB with 16 bits per channel.
   */
  var RGB16:Int;
  /*
   * Lab with 8 bits per channel.
   */
  var LAB8:Int;
  /*
   * Lab with 16 bits per channel.
   */
  var LAB16:Int;
};
package jsx.photoshop;

/*
 * The PDF magnification type.
 */
typedef MagnificationType = {
  /*
   * Displays the image at actual size.
   */
  var ACTUALSIZE:Int;
  /*
   * Fits the image to the page.
   */
  var FITPAGE:Int;
};
package jsx.photoshop;

/*
 * The export options to use.
 */
typedef ExportType = {
  /*
   * Exports Photoshop paths as Adobe Illustrator files.
   */
  var ILLUSTRATORPATHS:Int;
  /*
   * Uses the save for web export options.
   */
  var SAVEFORWEB:Int;
};
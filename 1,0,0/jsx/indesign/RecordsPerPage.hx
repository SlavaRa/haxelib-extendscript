package jsx.indesign;

/*
 * The number of records per page.
 */
typedef RecordsPerPage = {
  /*
   * Places each record on a new page.
   */
  var SINGLE_RECORD:Float;
  /*
   * Places as many records as fit on a page.
   */
  var MULTIPLE_RECORD:Float;
};
package jsx.indesign;

/*
 * Resize constraints.
 */
typedef ResizeConstraints = {
  /*
   * Keep current value
   */
  var KEEP_CURRENT_VALUE:Float;
  /*
   * Keep current proportions
   */
  var KEEP_CURRENT_PROPORTIONS:Float;
  /*
   * Tall proportions
   */
  var TALL_PROPORTIONS:Float;
  /*
   * Wide proportions
   */
  var WIDE_PROPORTIONS:Float;
  /*
   * Inverse proportions
   */
  var INVERSE_PROPORTIONS:Float;
};
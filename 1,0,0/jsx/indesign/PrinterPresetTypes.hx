package jsx.indesign;

/*
 * Printer preset options.
 */
typedef PrinterPresetTypes = {
  /*
   * The default printer preset.
   */
  var DEFAULT_VALUE:Float;
  /*
   * A custom printer preset.
   */
  var CUSTOM:Float;
};
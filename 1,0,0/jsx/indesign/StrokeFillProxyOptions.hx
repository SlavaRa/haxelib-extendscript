package jsx.indesign;

/*
 * Options for the active stroke/fill proxy.
 */
typedef StrokeFillProxyOptions = {
  /*
   * Stroke proxy is active.
   */
  var STROKE:Float;
  /*
   * Fill proxy is active.
   */
  var FILL:Float;
};
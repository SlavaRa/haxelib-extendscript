package jsx.indesign;

/*
 * Change bar location options.
 */
typedef ChangebarLocations = {
  /*
   * Change bars are in the left margin.
   */
  var LEFT_ALIGN:Float;
  /*
   * Change bars are in the right margin.
   */
  var RIGHT_ALIGN:Float;
};
package jsx.indesign;

/*
 * Grid view options.
 */
typedef GridViewSettings = {
  /*
   * Grid view.
   */
  var GRID_VIEW_ENUM:Float;
  /*
   * ZN view.
   */
  var ZN_VIEW_ENUM:Float;
  /*
   * Align view.
   */
  var ALIGN_VIEW_ENUM:Float;
  /*
   * Grid and ZN view.
   */
  var GRID_AND_ZN_VIEW_ENUM:Float;
};
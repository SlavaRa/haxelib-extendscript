package jsx.indesign;

/*
 * Story type options.
 */
typedef StoryTypes = {
  /*
   * The story is a regular text story.
   */
  var REGULAR_STORY:Float;
  /*
   * The story is a table of contents.
   */
  var TOC_STORY:Float;
  /*
   * The story is an index.
   */
  var INDEXING_STORY:Float;
};
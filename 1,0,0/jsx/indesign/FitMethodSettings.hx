package jsx.indesign;

/*
 * Fit method settings.
 */
typedef FitMethodSettings = {
  /*
   * Fit to predefined settings.
   */
  var FIT_PREDEFINED_SETTINGS:Float;
  /*
   * Fit to given width and height.
   */
  var FIT_GIVEN_WIDTH_AND_HEIGHT:Float;
  /*
   * Fit to given scale percentage.
   */
  var FIT_GIVEN_SCALE_PERCENTAGE:Float;
};
package jsx.illustrator;

typedef SVGCSSPropertyLocation = {
  /*
   * Style attributes with entity reference.
   */
  var ENTITIES:Int;
  var STYLEATTRIBUTES:Int;
  var STYLEELEMENTS:Int;
  var PRESENTATIONATTRIBUTES:Int;
};